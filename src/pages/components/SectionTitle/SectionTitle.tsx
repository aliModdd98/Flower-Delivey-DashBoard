const SectionTitle = ({ title }: { title: string }) => {
    return (
      <h2 className="font-semibold text-[34px] md:text-[50px] leading-[40.8px] md:leading-[60px]">
        {title}
      </h2>
    );
  };
  
  export default SectionTitle;
  